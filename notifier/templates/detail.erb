### Migration: <%= migration['migration'] %>

* Duration: <%= walltime(migration) %>
* Database size change: <%= total_size_change(migration) %>

% if migration['query_statistics']
<%= render_pgss_table(migration['query_statistics']) %>
% end
