| Clone ID | Clone Created At | Clone Data Timestamp | Expected Removal Time |
| -------- | ---------------- | -------------------- | --------------------- |
<% clone_details.each do |clone| %>
| [`<%= clone.cloneId %>`](https://console.postgres.ai/gitlab/<%= clone.projectName %>/instances/<%= clone.instanceId %>/clones/<%= clone.cloneId %>) | <%= clone.createdAt %> | <%= clone.cloneStateTimestamp %> | <%= clone.expectedRemovalTime %> |
<% end %>
