| Migration | Total runtime | Result | DB size change | Note |
| --------- | ------------- | ------ | -------------- | ---- |
% all_migrations.each do |m|
| <%= m['migration'] %> | <%= walltime(m) %> | <%= success(m) %> | <%= total_size_change(m) %> | <%= (!filtered?(m)) ? '(unrelated to this MR, but merged and pending on GitLab.com)' : '' %> |
% end
