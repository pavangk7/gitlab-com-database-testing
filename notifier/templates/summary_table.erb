| Migration | Total runtime | Result | DB size change | Note |
| --------- | ------------- | ------ | -------------- | ---- |
% all_migrations.each do |m|
| <%= m.version %> | <%= walltime(m) %> | <%= success(m) %> | <%= total_size_change(m) %> | <%= (!m.intro_on_current_branch) ? '(unrelated to this MR, but merged and pending on GitLab.com)' : '' %> |
% end
