| Migration | Total runtime | Result | DB size change |
| --------- | ------------- | ------ | -------------- |
% migrations_from_branch.each do |m|
| <%= m.version %> | <%= walltime(m) %> | <%= success(m) %> | <%= total_size_change(m) %> |
% end
