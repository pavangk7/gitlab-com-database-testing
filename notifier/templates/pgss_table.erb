% pgss_rows = pgss.queries.reject(&:excluded?)
Executed <%= format_time(pgss.excluded_query_duration) %> of common ignored queries.
% unless pgss_rows.empty?
| Query | Calls | Total Time | Max Time | Mean Time | Rows |
| ----- | ----- | ---------- | -------- | --------- | ---- |
% pgss_rows.each do |query|
| <pre><%= query.formatted_query %></pre> | <%= format_int(query.calls) %> | <%= format_time(query.total_time) %> | <%= format_time(query.max_time) %> | <%= format_time(query.mean_time) %> | <%= format_int(query.rows) %> |
% end
% end
